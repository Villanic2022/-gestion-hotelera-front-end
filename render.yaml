services:
  - type: web
    name: easycheck-frontend
    env: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    rootDir: .
    envVars:
      - key: NODE_VERSION
        value: "20"